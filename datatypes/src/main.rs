fn main() {
    let mut x = 4; //can declare variable type with let mut x: i32 for 32 bit signed int
    //floats are f32 or f64 
    //chars UTF-8 or ASCII 
    //bool type works with true or flase lower case
    println!("value of x is {}",x);
    x = "hello world" //static type - cant change data type after declared
    println!("value of x is {}",x);
}
